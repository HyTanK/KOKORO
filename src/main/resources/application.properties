spring.application.name=kokoro

# H2コンソールを使えるようにする
spring.h2.console.enabled=true

# 【修正】Render無料枠用のインメモリモード（ディスク書き込みエラーを回避）
spring.datasource.url=jdbc:h2:mem:sampledb;DB_CLOSE_DELAY=-1
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# 【修正】起動時に毎回テーブルを綺麗に作り直す設定
spring.jpa.hibernate.ddl-auto=create-drop

# 【追加】起動時に毎回「data.sql」の初期データを自動投入する設定
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

# ポート番号（Renderの動的ポートに対応）
server.port=${PORT:8080}
